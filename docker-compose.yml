services:
  # Layout de GPUs (2x RTX 4000 Ada 20GB):
  #   GPU 0 → só llama-refine (dedicada; permite modelo maior no refine)
  #   GPU 1 → só stt-api (Whisper + pyannote, sem briga por VRAM)
  stt-api:
    build: .
    ports: ["4321:8000"]
    environment:
      - CUDA_VISIBLE_DEVICES=1
      - WHISPER_GPUS=cuda:0  # dentro do container só existe cuda:0 (= física 1)
      - HF_TOKEN=${HF_TOKEN:-}
    volumes:
      - ./models:/app/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 2
              capabilities: [gpu]

  llama-refine:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    ports: ["1234:8080"]
    entrypoint: /entrypoint-refine.sh
    command:
      - --port
      - "8080"
      - --host
      - "0.0.0.0"
      - --n-gpu-layers
      - "-1"
      - --main-gpu
      - "0"
      - --parallel
      - "8"
      - --cont-batching
      - --flash-attn
      - "on"
      - --ctx-size
      - "32768"
    volumes:
      - ./models:/models
      - ./entrypoint-refine.sh:/entrypoint-refine.sh:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - HF_TOKEN=${HF_TOKEN:-}
